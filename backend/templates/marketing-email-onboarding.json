{
  "name": "marketing-email-onboarding",
  "version": "1.0",
  "nodes": [
    {
      "id": "send_welcome_email",
      "type": "email",
      "config": {
        "to": ["{{ node_results.get_profile.email }}"],
        "subject": "Welcome!",
        "body": "Hi {{ node_results.get_profile.first_name }}, welcome...",
        "original_config": {
          "to": ["{{ node_results.get_profile.email }}"],
          "subject": "Welcome!",
          "body": "Hi {{ node_results.get_profile.first_name }}, welcome..."
        }
      }
    }
  ]
}
